{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build and debug tests",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/ft_containers",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build",
        },
    ]
}
